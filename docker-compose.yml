services:
  app:
    build: .
    image: ip-checker:latest
    container_name: ip-checker
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      APP_URL: ${APP_URL:-http://208.84.102.204}
